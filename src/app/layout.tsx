import '../design-system/styles/base.css'; import type {Metadata} from 'next'; export const metadata:Metadata={title:'INOPNC',description:'INOPNC operations foundation',manifest:'/manifest.webmanifest'}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="ko" data-seed data-seed-color-mode="light-only"><body>{children}</body></html>}
