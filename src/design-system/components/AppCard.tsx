export function AppCard({children,className,padding=24}:{children:React.ReactNode;className?:string;padding?:number}){return <section className={`card ${className||''}`} style={{padding}}>{children}</section>}
