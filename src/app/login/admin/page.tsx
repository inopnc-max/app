'use client'; import {createClient} from '@/core/database/supabase/browser'; import {useState} from 'react';
export default function AdminLogin(){const [error,setError]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const c=createClient();if(!c)return;const {data}=await c.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(!data.user){setError(true);return;}window.location.href='/admin/mfa';}return <main style={{maxWidth:420,margin:'80px auto',padding:'0 16px'}}><h1>관리자 로그인</h1><form onSubmit={submit}><input name="email" type="email" required/><input name="password" type="password" required/><button type="submit">로그인</button></form>{error&&<p>로그인 정보를 확인해 주세요.</p>}</main>}
