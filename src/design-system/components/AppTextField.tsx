import type {InputHTMLAttributes} from 'react';
type Props=InputHTMLAttributes<HTMLInputElement>&{label:string;error?:string;description?:string};
export function AppTextField({label,error,description,id,...props}:Props){const fieldId=id||props.name;const messageId=error?`${fieldId}-error`:description?`${fieldId}-description`:undefined;return <label htmlFor={fieldId} style={{display:'grid',gap:8}}>{label}<input id={fieldId} {...props} aria-invalid={!!error} aria-describedby={messageId} style={{minHeight:44,border:'1px solid var(--color-border)',borderRadius:6,padding:'0 12px'}} />{description&&!error&&<small id={`${fieldId}-description`}>{description}</small>}{error&&<small id={`${fieldId}-error`} style={{color:'var(--color-error)'}}>{error}</small>}</label>}
