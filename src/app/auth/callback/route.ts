import {NextResponse} from 'next/server';import {createClient} from '@/core/database/supabase/server';import {resolveCurrentAccessContext} from '@/core/auth/resolver';import {resolvePostAuthDestination} from '@/core/auth/routing';
export async function GET(request:Request){const u=new URL(request.url),code=u.searchParams.get('code');if(!code)return NextResponse.redirect(new URL('/auth/error',u));const c=createClient();if(!c)return NextResponse.redirect(new URL('/auth/error',u));const x=await c.auth.exchangeCodeForSession(code);if(x.error)return NextResponse.redirect(new URL('/auth/error',u));const context=await resolveCurrentAccessContext();return NextResponse.redirect(new URL(context?resolvePostAuthDestination(context):'/account/pending',u));}
