import {requireAdminAal2,requirePermission} from '@/core/auth/guards';import {AppCard,AppPage} from '@/design-system';export default async function AccessPage(){await requireAdminAal2();await requirePermission('ACCESS_REQUEST_REVIEW');return <AppPage persona="admin" width="read"><AppCard><h1>파트너 접근 승인</h1><p>대기 중인 파트너 요청을 검토합니다.</p><p>승인 또는 거절을 선택하면 원자적 서버 RPC로 처리됩니다.</p></AppCard></AppPage>}
