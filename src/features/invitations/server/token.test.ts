import {describe,expect,it} from 'vitest';import {generateInvitationToken,hashInvitationToken,isValidInvitationToken} from './token';
describe('invitation tokens',()=>{it('generates secure unique tokens',()=>{const a=generateInvitationToken(),b=generateInvitationToken();expect(a).not.toBe(b);expect(a.length).toBeGreaterThanOrEqual(43);expect(hashInvitationToken(a)).not.toBe(a);});it('hashes deterministically and validates format',()=>{const t=generateInvitationToken();expect(hashInvitationToken(t)).toBe(hashInvitationToken(t));expect(isValidInvitationToken(t)).toBe(true);expect(isValidInvitationToken('bad')).toBe(false);});});
