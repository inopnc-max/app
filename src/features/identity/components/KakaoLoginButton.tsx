'use client';import {useState} from 'react';import {createClient} from '@/core/database/supabase/browser';import {AppButton} from '@/design-system';export function KakaoLoginButton(){const [busy,setBusy]=useState(false);return <AppButton disabled={busy} onClick={async()=>{setBusy(true);const c=createClient();if(c)await c.auth.signInWithOAuth({provider:'kakao',options:{redirectTo:`${window.location.origin}/auth/callback`}});}}>{busy?'이동 중...':'카카오로 계속하기'}</AppButton>}
