import type {ButtonHTMLAttributes} from 'react';
type Props=ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger';size?:'medium'|'large';fullWidth?:boolean;loading?:boolean};
export function AppButton({children,variant='primary',size='medium',fullWidth,loading,disabled,...props}:Props){const background=variant==='primary'?'var(--role-action)':variant==='danger'?'var(--color-error)':variant==='secondary'?'var(--role-tint)':'transparent';const color=variant==='secondary'?'var(--color-text)':variant==='ghost'?'var(--role-action)':'var(--role-on-action)';return <button {...props} disabled={disabled||loading} aria-busy={loading||undefined} style={{background,color,border:variant==='ghost'?'1px solid var(--color-border)':0,borderRadius:6,minHeight:size==='large'?48:44,padding:'0 18px',fontWeight:600,width:fullWidth?'100%':undefined,opacity:disabled||loading?.6:1}}>{loading?'처리 중...':children}</button>}
