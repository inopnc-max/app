import {requireAuthenticatedUser} from '@/core/auth/guards';import {redirect} from 'next/navigation';import MfaClient from './MfaClient';
export default async function MfaPage(){const c=await requireAuthenticatedUser();if(c.persona!=='admin')redirect('/account/forbidden');if(c.accountStatus!=='active')redirect(`/account/${c.accountStatus}`);if(c.aal==='aal2')redirect('/admin');return <MfaClient/>}
